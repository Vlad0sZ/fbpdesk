name: FBP Branch Build

on:
  push:
    branches:
      - fbp
  workflow_dispatch:

jobs:
  build-fbp:
    uses: ./.github/workflows/flutter-build.yml
    secrets: inherit
    with:
      upload-artifact: true
      upload-tag: "fbp-build"
